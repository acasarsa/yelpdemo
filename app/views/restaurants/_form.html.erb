<%= form_with(model: restaurant) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
        <% restaurant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :name, class: "col-sm-2 col-form-label" %>
    <%= form.text_field :name, class: "form-control g-3" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :address, class: "col-sm-2 col-form-label" %>
    <%= form.text_field :address, class: "form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :phone, class: "col-sm-2 col-form-label" %>
    <%= form.text_field :phone, class: "form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :website, class: "col-sm-2 col-form-label"%>
    <%= form.text_field :website, class: "form-control" %>
  </div>

  <div class="actions mb-3">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
